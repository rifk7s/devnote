import { Logout } from "@/components/logout";

export default function Page() {
    return (
        <div>
            Dashboard
            <Logout />
            {/* Add more dashboard content here */}
        </div>
    );
}